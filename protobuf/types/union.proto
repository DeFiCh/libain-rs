syntax = "proto3";
package union;  // group certain "either-or" types in this namespace

import "types/tx.proto";

message Transaction {
    oneof info {
        string hash = 1; // Transaction hash
        types.RawTransaction raw = 2; // Raw transaction data
    }
}

syntax = "proto3";
package types;

message AddressInput {
    string label = 1; // [default: empty]
    string type = 2; // [default: empty]
    string wallet = 3; // [default: empty]
}

message AddressResult {
    string address = 1;
}
